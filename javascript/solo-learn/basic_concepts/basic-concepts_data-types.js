// Fill in the blanks to declare a variable age and assign it the number 18:
let age = 18;

// A floating point number is allowed to have a decimal place

/* You can use quotes inside a string, as long as they don't match the quotes surrounding the string.
/* In order to create a string, we need to put the text inside... */
const text = 'My name is "John"';

// The backslash (\) escape character turns special characters into string characters.
const sayHello = "Hello world! 'I am a JavaScript programmer.'";

// \n = new line

const s = 'I think "JavaScript is easy"';
document.write(s);

// The Boolean data type accepts only two values; true, and false.
